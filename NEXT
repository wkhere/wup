+ ignore zero-length file
+ don't overwrite by default; special url or header for overwrite
+ return 403 for overwrite attempt
+ use http.Error(w, e, 500)
+ upload to dir given by DESTDIR envvar; fallback to systems temp
- fixme: for /multi/chunk/path only the last part is taken
- -HUP-proof
- logs
